curl -X GET "$URL/health" \
  -H "Content-Type: application/json" \
  -H "Modal-Key: $MODAL_KEY" \
  -H "Modal-Secret: $MODAL_SECRET" \

# curl -X GET "$URL/ready" \
#   -H "Content-Type: application/json" \
#   -H "Modal-Key: $MODAL_KEY" \
#   -H "Modal-Secret: $MODAL_SECRET" \


